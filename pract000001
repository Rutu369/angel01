10. Write a Python program to check set of vector span given vector
space

import numpy as np
v1 = [1, 1, 1]
v2 = [1, 2, 3]
v3 = [2, -1, 1]
b = [1, -2, 5]

A = np.column_stack((v1, v2, v3))
solution = np.linalg.solve(A, b)

print("Coefficients (c1,c2,c3):")
print(solution)

Output:

Coefficients (c1,c2,c3):
[-6. 3. 2.]
